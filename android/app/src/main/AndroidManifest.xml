<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 1. 기본 권한 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- 2. 진동 권한 (알림 올 때 진동 울리려면 필요) -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- 3. 재부팅 감지 권한 (폰 껐다 켜도 알람 유지하려면 필수) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- 4. 알림 권한 (Android 13+ 필수) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- 5. 정확한 알람 권한 (예약 알림용, Android 12+ 필수) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- 6. 정확한 알람 사용 권한 (Android 13+ 필수) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- 7. 기기 절전 모드 해제 권한 (알람 실행 보장) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 8. 배터리 최적화 제외 권한 (Android 12+) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 9. WorkManager 용 권한 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:label="모두의 할일"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Firebase 아이콘/채널 설정 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ★ 중요: 최신 버전 라이브러리는 Receiver가 자동으로 병합되므로 
             수동으로 <receiver> 태그를 넣지 마세요. 오히려 에러가 납니다. ★ -->

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>